class SignupData {
  String username;
  String email;
  String password;
  // Add more properties as needed for your signup process

  SignupData({required this.username, required this.email, required this.password});
}